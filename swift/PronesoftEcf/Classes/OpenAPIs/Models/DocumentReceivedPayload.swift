//
// DocumentReceivedPayload.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@available(*, deprecated, renamed: "PronesoftEcfAPI.DocumentReceivedPayload")
public typealias DocumentReceivedPayload = PronesoftEcfAPI.DocumentReceivedPayload

extension PronesoftEcfAPI {

/** Payload del evento &#x60;document.received&#x60;. Se dispara cuando tu empresa recibe un e-CF de un proveedor. */
public struct DocumentReceivedPayload: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case received = "received"
        case rejected = "rejected"
    }
    /** Número de comprobante fiscal electrónico. */
    public var encf: String
    /** RNC del emisor del documento. */
    public var senderRnc: String
    /** RNC del receptor del documento. */
    public var receiverRnc: String
    /** Monto total del documento. */
    public var totalAmount: Double
    /** Fecha de emisión del documento. */
    public var issueDate: Date
    /** Fecha de recepción del documento. */
    public var receivedAt: Date
    /** Estado del documento recibido. */
    public var status: Status
    /** Razón del rechazo (solo si status es 'rejected'). */
    public var rejectionReason: String?

    public init(encf: String, senderRnc: String, receiverRnc: String, totalAmount: Double, issueDate: Date, receivedAt: Date, status: Status, rejectionReason: String? = nil) {
        self.encf = encf
        self.senderRnc = senderRnc
        self.receiverRnc = receiverRnc
        self.totalAmount = totalAmount
        self.issueDate = issueDate
        self.receivedAt = receivedAt
        self.status = status
        self.rejectionReason = rejectionReason
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case encf
        case senderRnc
        case receiverRnc
        case totalAmount
        case issueDate
        case receivedAt
        case status
        case rejectionReason
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(encf, forKey: .encf)
        try container.encode(senderRnc, forKey: .senderRnc)
        try container.encode(receiverRnc, forKey: .receiverRnc)
        try container.encode(totalAmount, forKey: .totalAmount)
        try container.encode(issueDate, forKey: .issueDate)
        try container.encode(receivedAt, forKey: .receivedAt)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(rejectionReason, forKey: .rejectionReason)
    }
}

}
