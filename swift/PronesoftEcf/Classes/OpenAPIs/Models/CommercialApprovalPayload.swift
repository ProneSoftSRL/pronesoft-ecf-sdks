//
// CommercialApprovalPayload.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@available(*, deprecated, renamed: "PronesoftEcfAPI.CommercialApprovalPayload")
public typealias CommercialApprovalPayload = PronesoftEcfAPI.CommercialApprovalPayload

extension PronesoftEcfAPI {

/** Payload del evento &#x60;commercial.approval&#x60;. Se dispara cuando una aprobación comercial es actualizada. */
public struct CommercialApprovalPayload: Codable, JSONEncodable, Hashable {

    public enum ApprovalStatus: String, Codable, CaseIterable {
        case approved = "approved"
        case rejected = "rejected"
    }
    public var encf: String
    public var senderRnc: String
    public var receiverRnc: String
    public var totalAmount: Double
    public var issueDate: Date
    public var approvalStatus: ApprovalStatus
    public var approvalDate: Date
    /** Código de rechazo (solo si approvalStatus es 'rejected'). */
    public var rejectionCode: String?
    /** Descripción del rechazo (solo si approvalStatus es 'rejected'). */
    public var rejectionDescription: String?

    public init(encf: String, senderRnc: String, receiverRnc: String, totalAmount: Double, issueDate: Date, approvalStatus: ApprovalStatus, approvalDate: Date, rejectionCode: String? = nil, rejectionDescription: String? = nil) {
        self.encf = encf
        self.senderRnc = senderRnc
        self.receiverRnc = receiverRnc
        self.totalAmount = totalAmount
        self.issueDate = issueDate
        self.approvalStatus = approvalStatus
        self.approvalDate = approvalDate
        self.rejectionCode = rejectionCode
        self.rejectionDescription = rejectionDescription
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case encf
        case senderRnc
        case receiverRnc
        case totalAmount
        case issueDate
        case approvalStatus
        case approvalDate
        case rejectionCode
        case rejectionDescription
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(encf, forKey: .encf)
        try container.encode(senderRnc, forKey: .senderRnc)
        try container.encode(receiverRnc, forKey: .receiverRnc)
        try container.encode(totalAmount, forKey: .totalAmount)
        try container.encode(issueDate, forKey: .issueDate)
        try container.encode(approvalStatus, forKey: .approvalStatus)
        try container.encode(approvalDate, forKey: .approvalDate)
        try container.encodeIfPresent(rejectionCode, forKey: .rejectionCode)
        try container.encodeIfPresent(rejectionDescription, forKey: .rejectionDescription)
    }
}

}
