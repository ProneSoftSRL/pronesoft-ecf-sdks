//
// DocumentStatusChangedPayload.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@available(*, deprecated, renamed: "PronesoftEcfAPI.DocumentStatusChangedPayload")
public typealias DocumentStatusChangedPayload = PronesoftEcfAPI.DocumentStatusChangedPayload

extension PronesoftEcfAPI {

/** Payload inside &#x60;data&#x60; when event is &#x60;document.status_changed&#x60;. Use &#x60;documentId&#x60; to poll GET /{environment}/ecf/status/{id} if needed. &#x60;issuerRnc&#x60; identifies the company that issued the document (useful in multi-company setups where an associated company sent it).  */
public struct DocumentStatusChangedPayload: Codable, JSONEncodable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case registered = "REGISTERED"
        case toSend = "TO_SEND"
        case waitingResponse = "WAITING_RESPONSE"
        case finished = "FINISHED"
    }
    public enum LegalStatus: String, Codable, CaseIterable {
        case accepted = "ACCEPTED"
        case acceptedWithObservations = "ACCEPTED_WITH_OBSERVATIONS"
        case rejected = "REJECTED"
        case error = "ERROR"
    }
    /** Internal document ID for polling. */
    public var documentId: UUID
    /** e-NCF fiscal number. */
    public var encf: String
    /** RNC of the issuing company (associated or main). */
    public var issuerRnc: String
    /** Public lifecycle status. */
    public var status: Status
    /** Fiscal result from DGII. Only present when status=FINISHED. */
    public var legalStatus: LegalStatus?
    /** Tipo de documento code (31=Invoice, 32=Credit Note, etc.) */
    public var documentType: String?
    /** DGII tracking ID. */
    public var trackId: String?
    /** Human-readable DGII response message. */
    public var dgiiMessage: String?

    public init(documentId: UUID, encf: String, issuerRnc: String, status: Status, legalStatus: LegalStatus? = nil, documentType: String? = nil, trackId: String? = nil, dgiiMessage: String? = nil) {
        self.documentId = documentId
        self.encf = encf
        self.issuerRnc = issuerRnc
        self.status = status
        self.legalStatus = legalStatus
        self.documentType = documentType
        self.trackId = trackId
        self.dgiiMessage = dgiiMessage
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case documentId
        case encf
        case issuerRnc
        case status
        case legalStatus
        case documentType
        case trackId
        case dgiiMessage
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(documentId, forKey: .documentId)
        try container.encode(encf, forKey: .encf)
        try container.encode(issuerRnc, forKey: .issuerRnc)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(legalStatus, forKey: .legalStatus)
        try container.encodeIfPresent(documentType, forKey: .documentType)
        try container.encodeIfPresent(trackId, forKey: .trackId)
        try container.encodeIfPresent(dgiiMessage, forKey: .dgiiMessage)
    }
}

}
