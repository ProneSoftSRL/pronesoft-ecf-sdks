//
// SecurityNewLoginPayload.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

@available(*, deprecated, renamed: "PronesoftEcfAPI.SecurityNewLoginPayload")
public typealias SecurityNewLoginPayload = PronesoftEcfAPI.SecurityNewLoginPayload

extension PronesoftEcfAPI {

/** Payload del evento &#x60;security.new_login&#x60;. Se detectó un inicio de sesión desde una IP o dispositivo nuevo. */
public struct SecurityNewLoginPayload: Codable, JSONEncodable, Hashable {

    public var ip: String
    public var userAgent: String
    /** Ciudad detectada por geolocalización. */
    public var city: String?

    public init(ip: String, userAgent: String, city: String? = nil) {
        self.ip = ip
        self.userAgent = userAgent
        self.city = city
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ip
        case userAgent
        case city
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(ip, forKey: .ip)
        try container.encode(userAgent, forKey: .userAgent)
        try container.encodeIfPresent(city, forKey: .city)
    }
}

}
