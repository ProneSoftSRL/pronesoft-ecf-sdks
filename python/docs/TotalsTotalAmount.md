# TotalsTotalAmount


## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from pronesoft_ecf.models.totals_total_amount import TotalsTotalAmount

# TODO update the JSON string below
json = "{}"
# create an instance of TotalsTotalAmount from a JSON string
totals_total_amount_instance = TotalsTotalAmount.from_json(json)
# print the JSON string representation of the object
print(TotalsTotalAmount.to_json())

# convert the object into a dict
totals_total_amount_dict = totals_total_amount_instance.to_dict()
# create an instance of TotalsTotalAmount from a dict
totals_total_amount_from_dict = TotalsTotalAmount.from_dict(totals_total_amount_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


